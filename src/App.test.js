// import { render, screen } from '@testing-library/react';
// import App from './App';

// test('renders the quote button', () => {
//   render(<App />);
//   const buttonElement = screen.getByRole('button', { name: /new quote/i });
//   expect(buttonElement).toBeInTheDocument();
// });
